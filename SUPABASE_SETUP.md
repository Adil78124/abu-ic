# Инструкция по настройке Supabase для админки новостей

## Шаг 1: Настройка Row Level Security (RLS) для таблицы `news`

### Откройте SQL Editor в Supabase:

1. Зайдите в ваш проект Supabase: https://supabase.com/dashboard/project/aeewpulwnamwavtejlzq
2. Перейдите в раздел **SQL Editor** (в левом меню)
3. Создайте новый запрос

### Выполните SQL команды:

Скопируйте и выполните команды из файла `supabase-setup.sql` или выполните их по очереди:

#### 1. Включаем RLS для таблицы:
```sql
ALTER TABLE public.news ENABLE ROW LEVEL SECURITY;
```

#### 2. Создаём политики для чтения, записи, обновления и удаления:

```sql
-- Политика для чтения (всем разрешено)
CREATE POLICY "Разрешить чтение новостей всем"
ON public.news
FOR SELECT
USING (true);

-- Политика для добавления новостей
CREATE POLICY "Разрешить добавление новостей анонимным пользователям"
ON public.news
FOR INSERT
TO anon
WITH CHECK (true);

-- Политика для обновления новостей
CREATE POLICY "Разрешить обновление новостей анонимным пользователям"
ON public.news
FOR UPDATE
TO anon
USING (true)
WITH CHECK (true);

-- Политика для удаления новостей
CREATE POLICY "Разрешить удаление новостей анонимным пользователям"
ON public.news
FOR DELETE
TO anon
USING (true);
```

## Шаг 2: Настройка Storage для bucket `news-images`

### Убедитесь, что bucket создан и публичный:

1. Перейдите в раздел **Storage** → **Buckets**
2. Если bucket `news-images` не существует, создайте его:
   - Нажмите **New bucket**
   - Название: `news-images`
   - **Public bucket**: ✅ Включите (важно!)
   - Нажмите **Create bucket**

### Настройте политики Storage:

Выполните эти команды в SQL Editor:

```sql
-- Политика для загрузки файлов
CREATE POLICY "Разрешить загрузку файлов в news-images"
ON storage.objects
FOR INSERT
TO anon
WITH CHECK (bucket_id = 'news-images' AND auth.role() = 'anon');

-- Политика для чтения файлов (публичный доступ)
CREATE POLICY "Разрешить публичное чтение файлов из news-images"
ON storage.objects
FOR SELECT
TO public
USING (bucket_id = 'news-images');

-- Политика для удаления файлов
CREATE POLICY "Разрешить удаление файлов из news-images"
ON storage.objects
FOR DELETE
TO anon
USING (bucket_id = 'news-images' AND auth.role() = 'anon');
```

## Шаг 3: Проверка настроек

### Проверьте RLS на таблице:

1. Перейдите в **Table Editor** → **news**
2. Убедитесь, что RLS включен (должна быть иконка замка)
3. Проверьте, что политики созданы в разделе **Policies**

### Проверьте Storage:

1. Перейдите в **Storage** → **Buckets** → **news-images**
2. Убедитесь, что bucket **публичный** (Public bucket: ✅)
3. Проверьте политики в разделе **Policies**

## Возможные ошибки и их решение

### Ошибка: "policy already exists"

Если политика уже существует, удалите её сначала:

```sql
-- Для таблицы news
DROP POLICY "название_политики" ON public.news;

-- Для Storage
DROP POLICY "название_политики" ON storage.objects;
```

### Ошибка: "bucket does not exist"

Создайте bucket вручную через интерфейс:
1. **Storage** → **Buckets** → **New bucket**
2. Название: `news-images`
3. Включите **Public bucket**
4. Создайте bucket

### Ошибка доступа при загрузке изображений

Убедитесь, что:
1. Bucket `news-images` существует и публичный
2. Политики Storage созданы и активны
3. В коде используется правильный bucket ID: `news-images`

## Альтернативный вариант (для продакшена)

Для более безопасной настройки используйте service_role key вместо anon key:

1. Создайте отдельный файл конфигурации для админки
2. Используйте service_role key (храните его в безопасности!)
3. Настройте более строгие RLS политики

## Готово! 🎉

После выполнения этих шагов ваша админка должна работать с Supabase.

Проверьте:
- ✅ Можете добавлять новости
- ✅ Можете редактировать новости
- ✅ Можете удалять новости
- ✅ Изображения загружаются в Storage
- ✅ Новости отображаются в списке

